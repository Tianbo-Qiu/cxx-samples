#pragma once

#include <vector>

/**
 * Return the number of good pairs.
 * (i, j) is good if nums[i] == nums[j] and i < j
 */
int nogp(std::vector<int> nums);

/**
 * if you see it as counting numbers...
 */
int nogp2(std::vector<int> nums);
